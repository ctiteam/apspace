import { TimeFrameFilterPipe } from './time-frame-filter.pipe';

describe('TimeFrameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFrameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
