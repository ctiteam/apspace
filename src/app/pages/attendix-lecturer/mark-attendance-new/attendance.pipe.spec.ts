import { AttendancePipe } from './attendance.pipe';

describe('AttendancePipe', () => {
  it('create an instance', () => {
    const pipe = new AttendancePipe();
    expect(pipe).toBeTruthy();
  });
});
