import { ClasscodesFilterPipe } from './classcodes-filter.pipe';

describe('ClasscodesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClasscodesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
