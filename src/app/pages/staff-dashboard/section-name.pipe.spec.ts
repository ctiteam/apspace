import { SectionNamePipe } from './section-name.pipe';

describe('SectionNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SectionNamePipe();
    expect(pipe).toBeTruthy();
  });
});
