import { OperationHoursFilterPipe } from './operation-hours-filter.pipe';

describe('OperationHoursFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OperationHoursFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
