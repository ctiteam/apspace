import { StrToColorPipe } from './str-to-color.pipe';

describe('StrToColorPipe', () => {
  it('create an instance', () => {
    const pipe = new StrToColorPipe();
    expect(pipe).toBeTruthy();
  });
});
