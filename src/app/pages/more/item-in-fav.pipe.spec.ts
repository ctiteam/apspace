import { ItemInFavPipe } from './item-in-fav.pipe';

describe('ItemInFavPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemInFavPipe();
    expect(pipe).toBeTruthy();
  });
});
